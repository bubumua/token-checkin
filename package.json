{
  "name": "cf-checkin-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "sync-secrets": "pwsh -NoProfile -File scripts/sync-secrets.ps1 -LoginIfNeeded",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250327.0",
    "typescript": "^5.8.2",
    "wrangler": "^4.35.0"
  }
}
